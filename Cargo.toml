[package]
name = "cdo"
description = "Runs a command in the given directory"
version = "0.1.0"
license = "MIT"
edition = "2021"

[dependencies]
anyhow = "1.0.71"
clap = { version = "4.3.4", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2.0.11"
tempfile = "3.6.0"
